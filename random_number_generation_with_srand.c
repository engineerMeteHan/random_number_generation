#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <conio.h>

int i = 0;
void main()
{
    // use rand() function to generate the number
    printf("The tandom number is: %d", rand());
    printf("\nThe tandom number is: %d", rand());
    printf("\nThe tandom number is: %d", rand());
    printf("\nThe tandom number is: %d", rand());
    getch();
}
